import { CaseAssignedSupervisorsList } from './case-assigned-supervisors-list';

describe('CaseAssignedSupervisorsList', () => {
  it('should create an instance', () => {
    expect(new CaseAssignedSupervisorsList()).toBeTruthy();
  });
});
